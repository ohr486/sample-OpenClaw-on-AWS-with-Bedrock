# OpenClaw Enterprise — Frontend Environment Variables (Vite)
# Copy this file to .env and fill in the values.
# Variables must start with VITE_ to be available in frontend code.

# Azure AD / Microsoft Entra ID (optional — required for "Sign in with Microsoft")
VITE_AZURE_CLIENT_ID=
VITE_AZURE_TENANT_ID=
